html
    head
        include ./partials/head.pug
        style(type='text/css').
            body {
            display: -ms-flexbox;
            display: -webkit-box;
            display: flex;
            -ms-flex-pack: center;
            -webkit-box-pack: center;
            justify-content: center;
            color: #fff;
            text-shadow: 0 .05rem .1rem rgba(0, 0, 0, .5);
            box-shadow: inset 0 0 5rem rgba(0, 0, 0, .5);
            }

            .cover-container {
            max-width: 42em;
            }

    body.background-image.text-center(style="background-blend-mode: soft-light;")
        .cover-container.d-flex.h-100.p-3.mx-auto.flex-column.align-self-center
            main.cover.inner(role="main")
                h1.cover-heading WHY I 
                    span.text-danger LIFT 
                    |.COM
                p.lead
                    | Coming Soon